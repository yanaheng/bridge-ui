// const Width=window.screen.width;
const Width=document.body.clientWidth;

export default {
    board: {
        width: Width * 0.6,
        height: Width * 0.6,
        top: Width * 0.2,
        left: Width * 0.2,
    },
}